# Workflow backup

The original project workflows were moved to `original-project/` so GitHub will not run them in this repository.

To restore them for merging back into the original project, copy or move the files from:

```sh
.github/workflows-backup/original-project/
```

back into:

```sh
.github/workflows/
```
